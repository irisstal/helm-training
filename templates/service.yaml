apiVersion: v1
kind: Service
metadata:
  name: {{ include "mongo-helm.fullname" . }}
  labels:
    {{- include "mongo-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mongo-helm.selectorLabels" . | nindent 4 }}
